# Frontend environment variables
# Copy this to .env.local and fill in your values

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Google OAuth (handled via backend — no frontend keys needed)
